.DS_Store
*.js.map
node_modules/
coverage/
npm-debug.log
stats.json
yarn-error.log

data/
db
.env
public/**/*
!public/index.html

# Email layouts, nested templates, navigation, and pages are required at
# runtime (Docker image + service boot). Keep them in git; public media
# assets still come from StretchShop-demo-data during image build / postinstall.

.vscode/
copyToDemoData.sh
*.log
.temp/
.temp/webhook.log
docs/openapi/generated.json
